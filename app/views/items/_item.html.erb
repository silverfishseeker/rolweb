<%# expected params: item,(optional) sobreescritura %>
<div class="carta">
    <%= render "items/item_inner", item: item, sobreescritura: local_assigns[:sobreescritura] %>
    <%= render("shared/image_with_link", image: item.image, cssclass: "carta-image carta-image-item") if item.image%>
</div>
