<% content_for :title, @x.nombre %>
<% only_admin_content do %>
    <div class="admin-actions">
        <%= link_to "Edit", edit_mob_path(@x), class: "btn btn-primary" %>
        <%= link_to "Destroy", mob_path(@x), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
    </div>
<% end %>

<div class="clase-main">
    <%= render "mob", mob: @x %>
<div>
<br>

<h1>Hablidades inherentes:</h1>
<%= render "habilidads/lista", habilidades: @x.habilidadsOfMob%>