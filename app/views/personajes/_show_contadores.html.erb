<%# Expected params: calculados, prefix %>
<% calculados.each do |contador| %>
  <div class="carta-contador">
    <input type="hidden"
        name="<%= prefix %>[calculados][<%= contador.id %>][id]"
        value="<%= contador.id %>">
    <% if contador.rango.present? %>
      <input type="number" title="<%= contador.nombre %>"
          name="<%= prefix %>[calculados][<%= contador.id %>][rango]"
          value="<%= contador.rango.valor %>"
          class="pj-cantidad autosave-input"
          data-update-url="<%= update_contador_rango_personaje_path(@x, calculado_id: contador.id) %>"
          data-update-param="rango"
          id="contador-rango-<%=contador.id%>">
      <b> / </b>
    <% end %>
    <input type="number" title="<%= contador.nombre %>"
        name="<%= prefix %>[calculados][<%= contador.id %>][base]"
        value="<%= contador.value %>"
        class="pj-cantidad autosave-input"
        data-update-url="<%= update_contador_base_personaje_path(@x, calculado_id: contador.id) %>"
        data-update-param="base"
        id="contador-base-<%=contador.id%>">
  </div>
<% end %>