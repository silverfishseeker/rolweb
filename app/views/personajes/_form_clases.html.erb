<%# Expected params: phClases, destroy, oculto %>
<div class="carta-grid_container carta-grid_container_form" id="clases_container_<%= oculto || destroy %>">
  <% phClases.each do |phClase| %>
    <%= render "form_clases_clase", phc: phClase, destroy: destroy, oculto: oculto %>
  <% end %>
</div>