<% content_for :title, "Error" %>
<div class="texto_info-margins">
  <h2>Ocurrió un error:</h2>

  <% if flash[:error] %>
    <div class="flash-error">
      <%= flash[:error] %>
    </div>
  <% else %>
    <div class="flash-error">
      <strong>Error desconocido</strong>
      <p>Por favor, inténtalo de nuevo más tarde o contacta al administrador.</p>
    </div>
  <% end %>

  <div style="margin-top: 2em;">
    <%= link_to "Volver atrás", request.referer || root_path, class: "btn btn-primary" %>
  </div>
</div>